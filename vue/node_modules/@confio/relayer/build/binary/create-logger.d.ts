import winston from 'winston';
import { LoggerFlags } from './types';
export interface Logger extends Omit<winston.Logger, 'child'> {
    child(options: {
        label: string;
    }): Logger;
}
export declare const levels: {
    error: number;
    warn: number;
    info: number;
    verbose: number;
    debug: number;
};
declare type Level = keyof typeof levels;
export declare const defaultLevel = "info";
export declare function resolveLevel(flags: LoggerFlags): [level: Level, invalidInputLevel: string | null];
export declare function createLogger(flags: LoggerFlags): Logger;
export {};
