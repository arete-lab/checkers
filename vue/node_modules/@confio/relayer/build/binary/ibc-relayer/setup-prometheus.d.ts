import client from 'prom-client';
import { Logger } from '../create-logger';
export declare type Metrics = {
    loopTotal: client.Counter<string>;
} | null;
export declare function setupPrometheus({ enabled, port, logger, }: {
    enabled: boolean;
    port: number;
    logger: Logger;
}): Metrics;
