import { Logger } from '../create-logger';
import { LoggerFlags } from '../types';
export declare function loggerWithErrorBoundary<T>(command: (flags: T, logger: Logger) => Promise<void>): (flags: T & LoggerFlags) => Promise<void>;
