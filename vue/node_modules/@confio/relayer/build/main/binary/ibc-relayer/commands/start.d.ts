import { Logger } from 'winston';
import { LoggerFlags } from '../../types';
declare type LoopFlags = {
    poll?: string;
    maxAgeSrc?: string;
    maxAgeDest?: string;
    once: boolean;
};
declare type Flags = {
    interactive: boolean;
    home?: string;
    src?: string;
    dest?: string;
    keyFile?: string;
    mnemonic?: string;
    srcConnection?: string;
    destConnection?: string;
    scanFromSrc?: string;
    scanFromDest?: string;
} & LoggerFlags & LoopFlags;
export declare function start(flags: Flags, logger: Logger): Promise<void>;
export {};
