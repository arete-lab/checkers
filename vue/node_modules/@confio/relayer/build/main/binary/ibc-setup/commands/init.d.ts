import { Logger } from 'winston';
declare type Flags = {
    readonly home?: string;
    readonly src?: string;
    readonly dest?: string;
};
export declare type Options = Required<Flags>;
export declare function init(flags: Flags, logger: Logger): Promise<void>;
export declare function run(options: Options, logger: Logger): Promise<void>;
export {};
