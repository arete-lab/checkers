import { Logger } from 'winston';
import { Chain } from '../../types';
export declare type Flags = {
    readonly interactive: boolean;
    readonly home?: string;
    readonly keyFile?: string;
    readonly mnemonic?: string;
};
export declare type Options = {
    readonly home: string;
    readonly mnemonic: string;
};
export declare function keysList(flags: Flags, logger: Logger): Promise<void>;
export declare function getAddresses(registryChains: Record<string, Chain>, mnemonic: string): Promise<[chain: string, data: Chain, address: string][]>;
export declare function run(options: Options, logger: Logger): Promise<void>;
