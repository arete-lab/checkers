import { resolveNumericOption } from './resolve-numeric-option';
export declare function resolveRequiredNumericOption(identifier: string): (...args: Parameters<typeof resolveNumericOption>) => number;
