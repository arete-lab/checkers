import { IbcClient } from '../../lib/ibcclient';
import { Logger } from '../../lib/logger';
import { Chain } from '../types';
export declare function signingClient(chain: Chain, mnemonic: string, logger?: Logger): Promise<IbcClient>;
